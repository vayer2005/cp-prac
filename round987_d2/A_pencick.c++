#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <stack>
#include <cmath>

using namespace std;


void solve() {
    long long n;
    cin >> n;
    vector<long long> vals(n,0);
    for (long long i = 0; i < n; i++) {
        cin >> vals[i];
    }


}
int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;

}