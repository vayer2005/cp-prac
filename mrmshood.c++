#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <cmath>
#include <queue>

using namespace std;

void solve() {
    int n; int d; int k;
    cin >> n >> d >> k;
    vector<int> counts(n, 0);
    int start; int end;
    for (int i = 0; i < k; i++) {
        cin >> start >> end;
    }



    

}

int main() {
    int t;
    cin >> t;

    while (t--) {
        solve();
    }

}
